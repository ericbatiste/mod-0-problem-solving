// Define strings array with upper & lower case letters.
// Itereate through array
// Call .toLowerCase method on each item.
// Print each modified array item.

var words = ["wAcKy", "WiLd", "WoNdErFuL", "wIsDoM"];
words.forEach((word) => console.log(word.toLowerCase()));

// Prints: "wacky" "wild" "wonderful" "wisdom"